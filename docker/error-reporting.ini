[PHP]
; Suppress E_DEPRECATED in dev — vendor libraries (delight-im/db) trigger
; compile-time deprecation notices in PHP 8.4 that output before headers,
; breaking sessions and page rendering.
error_reporting = E_ALL & ~E_DEPRECATED
