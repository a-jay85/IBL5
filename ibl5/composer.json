{
    "require": {
        "vimeo/psalm": "^4.16",
        "phpstan/phpstan": "^1.2",
        "symfony/filesystem": "^5.4"

    },
    "config": {
        "allow-plugins": {
            "composer/package-versions-deprecated": true
        }
    }
}
