# Worktree: phpstan-echo-die-cast-rules

This worktree's Docker instance is at `phpstan-echo-die-cast-rules.localhost`.
Use this hostname for all browser checks, curl, and E2E — never `main.localhost`.
